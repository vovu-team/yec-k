import { createNew } from "./createNew";
import { deleteByID } from "./deleteByID";
import { list } from "./list";
import { updateByID } from "./updateByID";
import { vote } from "./vote";

export const Contesters = {
  createNew,
  deleteByID,
  list,
  updateByID,
  vote,
};
